# Fongen Training Dependencies (AMD ROCm / MI300X)
# Install: pip install -r requirements.txt

# Core ML
torch>=2.5.0
transformers>=4.46.0
datasets>=3.0.0
accelerate>=1.0.0
peft>=0.13.0
trl>=0.12.0

# Unsloth for fast LoRA fine-tuning (AMD)
# See: https://unsloth.ai/docs/get-started/install/amd
unsloth[amd]>=2024.12.0

# GGUF export
gguf

# Data processing
pandas>=2.2.0
numpy>=1.26.0
sqlite-utils>=3.36.0

# Utilities
tqdm
jsonlines
pyarrow>=15.0.0
